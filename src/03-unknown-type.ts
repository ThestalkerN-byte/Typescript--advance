//unknow types
let prueba:unknown;

if(typeof prueba === 'string'){
    prueba.length  // una vez entrado a este if nos va  a permitir saber todos los metodos que le alcancen a este.
    //esto es posible siempre y cuando verifiquemos que tipo de dato podria llegar a ser, esto no pasa con un any.
    
}
